name: Email Action Test

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: 25ds2000022@ds.study.iitm.ac.in
        run: echo "Hello, world!"

      - name: Done
        run: echo "Action completed successfully."
